# Writting some code to help cook a gorgeous lasagna.
# The first function definition is given in the stub file, we completed the remaining function definitions ourself as well as defining any constants.


//Define the EXPECTED_BAKE_TIME
EXPECTED_BAKE_TIME = 40

def bake_time_remaining(elapsed_bake_time):
    //"""Calculate the remaining bake time."""
    return EXPECTED_BAKE_TIME - elapsed_bake_time

bake_time_remaining(30)

def preparation_time_in_minutes(number_of_layers):
    //"""Calculate the layers preping time."""
    return number_of_layers * 2

preparation_time_in_minutes(2)

def elapsed_time_in_minutes(number_of_layers, elapsed_bake_time):
    //"""Calculate the elapsed cooking time."""
    return (number_of_layers * 2) + elapsed_bake_time

elapsed_time_in_minutes(3,20)

//adding "notes" in the form of function docstrings.
"""Calculate the elapsed cooking time.
    
    Parameters:
        number_of_layers (int): The number of layers in the lasagna.
        elapsed_bake_time (int): Time the lasagna has been baking in the oven.
    
    Returns:
        int: The total time elapsed (in minutes) preparing and baking.

    This function takes two integers representing the number of lasagna 
    layers and the time already spent baking the lasagna. It calculates 
    the total elapsed minutes spent cooking (preparing + baking).
    
    """
